# Agenda

* Topic: Nightly job CDash posting https://my.cdash.org/index.php?project=Kokkos
  * Any updates, progress, or feedback using the framework for posting of nightly jobs?

* Topic: CI jobs and resources
  * Reshuffle of ORNL CI
  * Adding more SNL CI jobs

* Topic: Toward standardizing test processes
  * Any new cmake-presets related updates?
      * Example [gist](https://gist.github.com/rgayatri23/8bbbbce0e9591848105a6dda70559742) (thanks Rahul!)
  * Any other updates?

* Topic: Tested configurations:
  * Curently tested configuration: [CI, nightlies](https://github.com/kokkos/internal-documents/blob/master/ci_working_group/Automated_Testing.md)
    * Thanks for updating the new test configurations, please continue to add to the list as tests are added/modified
    * Feedback, updates, next steps?
  * Automated options to help track testing configurations:
    * Idea posted on kokkos-ci (thanks Paul!) - specify job info in CI files and push it to CDash with ctest -A (command line) or with ctest_submit FILES (script)
 
* Topic: Ability to skip CI builds
  * Site-specific options [gathered from PR 7754](https://github.com/kokkos/kokkos/pull/7754) (thanks Daniel!):
    * [skip appveyor] for Appveyor Windows build
    * [skip actions] for GitHub Actions workflow runs
    * [skip. ci] or [skip ci] for GitLab pipelines

# Notes

CDash
- Site names: make sure they are descriptive, the Site field on can be populated with a non-default by using `-DSITE=<sitename>`
- Build names: what convenvtion should we use? How to best balance readability with necessary options? Some goals
  1. Name needs to be easily parsable, so it can eventually be used by script for reproducibility - longer build names are fine
  2. Uniqueness of name, so it can be mapped to a specific build+configuration
- New Kokkos Kernels CDash project added for posting jobs (thanks Luc!): https://my.cdash.org/index.php?project=Kokkos%20Kernels

CI jobs and resources
ORNL CI Reshuffle
- New nodes available (A30 GPUs)
- Jenkins CI: Current stages consist of 1) Clang-Format 2) Build-1 (3 jobs) 3) Build-2 (9 jobs)
- Builds consist of mainline production jobs and experimental jobs - split into two pipelines
- ACTION ITEM : Next steps
  1. Create .jenkins directory with named pipelines
  2. Move OpenACC, OpenMPTarget, and Makefile-build to a separate pipeline
  3. Review remaining jobs (Hip - Bruno) (Sycl - Daniel)
  4. Review of build times - which jobs are longest, consistently using ccache, maximizing cores for parallel builds, ...

SNL New CI jobs
- Desired to add: 1 Hip, 1 Sycl on PVC, CPU-only testing SIMD ops (SPR?)
- Diversify the builds from existing jobs for expanded coverage, avoid duplicate jobs

Tested Configurations
- Improve readability of current table
- Eventual goal is storage in a database to extract and aggregate desired info
- Automating test output:
  - Using Ctest to drive the full job automatically populates the user-configuration under the CDash "Configure Command" field
  - Use of `-A <command line>` should do this as well when stages are run separately (Ctest only for unit testing)
- ACTION ITEM: Continue populating useful testing-related info to skip_testing.md , such as
  - Testing / site-specific info useful for developers
  - Info regarding CI WG meetings, how to join, overall mission
  - Eventually publish to external site for public view, what is a good location? Suggestions: "Developers" section under kokkos.org/Documentation , or [Jenkins special interest groups](https://www.jenkins.io/sigs/) ?

# Attendees

Damien Lebrun-Grandie
Daniel Arndt
Bruno Turcksin
Paul Zehner
Luc Berger-Vergiat
Nathan Ellingwood
