# Friday, September 18th, 2026 Kokkos-FFT Developer Meeting

https://zoom-lfx.platform.linuxfoundation.org/meeting/97642956440?password=9f648a4f-2083-4dec-ad84-ce8e28bd30c4

## Attendance

- Yuuichi Asahi
- Thibault Cimic
- Lawton Shoemake

## Community
  
## Testing

### CI

### Nightly

- [Kokkos FFT](https://my.cdash.org/index.php?project=Kokkos-FFT)' CDash

## Documentation, Communication Structures

## Release

## Callbacks
### Progress Since Last Meeting (August 21, 2026)

#### Draft
1. cuFFT legacy callback implementation. https://github.com/kokkos/kokkos-fft/pull/534

#### Completed
1. **Functor Strategy Prototype**
   - **Conclusion**: Functors **cannot bypass `MemcpyFromSymbol`**.
   - **Reason**: `operator()` is a **member function** → cannot be converted to a raw function pointer (required by C APIs like `*XtSetCallback`).

2. **`Callbackable` Trait**
   - **Status**: Implemented and tested.
   - **Files**:
     - `kokkos-fft/common/src/KokkosFFT_Traits.hpp` (trait + `Callbackable_v`).
     - `kokkos-fft/common/unit_test/Test_Traits.cpp` (tests for CUDA/HIP/Serial/OpenMP).
   - **Purpose**: Compile-time detection of callback support for `ExecutionSpace`.
   - **Test Results**:
     - ✅ CUDA: `CudaIsCallbackable` passes.
     - ✅ HIP: `HIPIsCallbackable` passes (tested on Adastra).
     - ✅ Serial/OpenMP: `SerialIsNotCallbackable`/`OpenMPIsNotCallbackable` pass.

3. **`MemcpyFromSymbol`**
   - **Design**: Recommended to be embedded inside a Callback wrapper class/function. (Same strategy as `KokkosFFT_*_types.hpp` wherein the HIP APIs are directly used when needed. General interfaces have not been introduced.)
   - Resolved 18/September.

#### Current Status:

1. MemcpyFromSymbol issue has been resolved, but FFT execute fails.
2. cuFFT wrapper MWE: https://godbolt.org/z/rh45jGTjv
3. Can we get a device symbol of functor?
4. Can we provide a templated symbol?

```C++
// Does not compile
template <typename T>
KOKKOS_IMPL_DEVICE_FUNCTION auto zero_pad_load_callback(void* dataIn, size_t
offset, void* callerInfo, void* sharedPointer) -> typename
KokkosFFT::Impl::fft_data_type<execution_space, T>::type { using data_type =
typename KokkosFFT::Impl::fft_data_type<execution_space, T>::type; auto*
callback_params = static_cast<Params*>(callerInfo); const data_type* in_data =
static_cast<const data_type*>(dataIn);

  // Zero-padding: return 0 for indices beyond original_size
  if (offset >= callback_params->original_size) {
    return static_cast<data_type>(0);
  }
  return in_data[offset];
}

template <typename T>
KOKKOS_IMPL_DEVICE_FUNCTION KokkosFFT::CallBackSymbolType<execution_space, T,
KokkosFFT::LoadCallback>::type d_load_callback_symbol =
zero_pad_load_callback<T>;
```
